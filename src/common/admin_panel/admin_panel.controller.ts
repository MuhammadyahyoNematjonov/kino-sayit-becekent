import { Controller } from '@nestjs/common';

@Controller('admin-panel')


export class AdminPanelController {
  // This controller can be extended with methods to handle admin panel routes
  // For example, you can add methods to manage users, roles, or other admin tasks
}       